# >>> hee gitignore base >>>
# Managed by: hee check gitignore --fix. Edit the base, not this block.
# Base: .github/gitignore/base.gitignore (the org baseline)
# Never negate one of these back in: in a public repo it publishes
# the file permanently -- see pr:609@human-execution-engine.
secrets/
.hee/secrets/
*.env
*.pem
*.p12
*.pfx
.netrc
id_rsa
id_ecdsa
id_ed25519
# <<< hee gitignore base <<<

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Node.js (if used later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Temporary files
*.tmp
*.temp
.cache/

# Coverage database -- generated by pytest-cov, never committed.
.coverage

# Editor-derived, never committed. Governance reaches this repo through the
# dotfiles SessionStart hook, per CLAUDE.md -- not through a copy in-tree.
.cursor/
